package `in`.projecteka.jataayu.registration.ui

import `in`.projecteka.jataayu.registration.viewmodel.RegistrationActivityViewModel
import org.junit.After
import org.junit.Before
import org.junit.Test
import org.junit.runner.RunWith
import org.mockito.junit.MockitoJUnitRunner

@RunWith(MockitoJUnitRunner::class)
class RegistrationOtpFragmentTest {

    private lateinit var viewModel: RegistrationActivityViewModel

    @Before
    fun setup() {

    }

    @After
    fun tearDown() {

    }

    @Test
    fun `when otp is expired`() {

    }
}