package `in`.projecteka.jataayu.user.account.listener

interface CredentialsInputListener{
    fun onUsernameEdit(username: String)
    fun onPasswordEdit(password: String)
    fun onAyushmanIdEdit(password: String)
}